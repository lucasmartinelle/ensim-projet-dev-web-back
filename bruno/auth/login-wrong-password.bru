meta {
  name: Login — mauvais mot de passe
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "player@example.com",
    "password": "wrongpassword"
  }
}

tests {
  const body = res.getBody();
  expect(body.message).to.equal('Identifiants invalides');
}

docs {
  ## Login — mauvais mot de passe

  **Réponse attendue :** erreur `Invalid credentials`
}
