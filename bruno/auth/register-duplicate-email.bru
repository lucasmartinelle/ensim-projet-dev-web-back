meta {
  name: Register — email dupliqué
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/auth/register
  body: json
  auth: none
}

body:json {
  {
    "email": "player@example.com",
    "username": "otherusername",
    "password": "password123"
  }
}

tests {
  const body = res.getBody();
  expect(body.message).to.equal('Email already used');
}

docs {
  ## Register — email dupliqué

  **Réponse attendue :** erreur `Email already used`
}
