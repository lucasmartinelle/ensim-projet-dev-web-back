meta {
  name: Register — email dupliqué
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/auth/register
  body: json
  auth: none
}

body:json {
  {
    "email": "player@example.com",
    "username": "playerone2",
    "password": "password123"
  }
}

docs {
  ## Register — email dupliqué

  Doit retourner une erreur si l'email est déjà utilisé.

  **Réponse attendue :** `500` `{ message: "Email already used" }`
}
