meta {
  name: Me — sans token
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/auth/me
  body: none
  auth: none
}

tests {
  expect(res.getStatus()).to.equal(401);
  expect(res.getBody().message).to.equal('Non autorisé');
}

docs {
  ## Me — sans token

  **Réponse attendue :** `401` `{ message: 'Unauthorized' }`
}
